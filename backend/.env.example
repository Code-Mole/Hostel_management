# Backend environment configuration (.env)

# MongoDB connection string (used by server in config/Db.js)
MONGO_URI=mongodb://localhost:27017/estatepro

# MongoDB connection string used by utils/createAdmin.js (keep in sync)
MONGODB_URI=mongodb://localhost:27017/estatepro

# Server listen port
# Note: server.js reads LOWERCASE `port`. Define both to be safe.
port=8000
PORT=8000